# devlowへの名称統一

2026-09-23。ユーザーの指定により、統括スキルの名称をdevlowへ統一した。旧名への互換入口は設けない。

スキルのディレクトリとfrontmatter、開発資料の配置と参照、orchからの接続、両製品の導入テンプレート、既存の作業記録内の名称・パス、Obsidianのテンプレート参照を更新した。配布物は正本から再生成し、両クライアントへ更新版を導入した。8工程の手順、CLIの処理、task.mdや知識の保存形式は変更していない。

既存の作業台帳は文字列を統一し、過去の検証ハッシュは当時の値を保持した。以前の配布作業の親unitについてdoctorが対象変更を検出することを確認した。過去の合格を改名後の確認として扱わず、今回の結果は以下に分けて記録する。

- 配布テスト5件、77 assertions成功。
- 型検査、配布物の全パス・内容照合、変更したスキル2件の形式検証、両製品のmanifest検証が成功。
- リポジトリの現行ファイルについて、旧識別名の本文・パス残存なし。Gitの履歴、依存パッケージ、人間用workは置換対象外。
- 開発資料とObsidianの169件のローカル参照が解決することを確認。
- 旧版でsetup済みの一時プロジェクトを両製品の新版へ更新。指示がdevlowを参照し、管理ブロック外の既存指示を保持すること、再適用で変化しないことを確認。

インストール済みキャッシュも、両製品とも115ファイルが配布物と一致し、devlowのみを持つことを確認した。

改名後の入口は[devlow](../../../skills/devlow/SKILL.md)。インストール済みスキルの読み込みは、新しいタスクまたはセッションで行う。
